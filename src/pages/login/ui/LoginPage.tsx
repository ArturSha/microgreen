import style from './LoginPage.module.css';

const LoginPage = () => {
  return (
    <section className={style.loginPage}>
      <form action=""></form>
    </section>
  );
};
export default LoginPage;
